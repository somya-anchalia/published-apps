[cybereason://default]
*This is how the Cybereason For Splunk is configured

base_url = <value>
* This is the base_url for the Modular Input to consume information from. Include Port.

cybereason_user = <value>
* This is the username for the Modular Input to consume information with

credential_realm = <value>
* This is the encrypted credential Realm to use when pulling the credential

endpoints = <value>
* These are the endpoints (comma separated) to retrieve

proxy_name = <value>
* This allows outbound configuration of a proxy.

ssl_verify = <value>
* Support only.